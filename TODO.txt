STUDY TIME:
    go through the sources:
        https://gemini.google.com/app/8775a3841376abf0
        https://claude.ai/chat/051a5137-934a-4088-adae-36f5438127c4

casey immediate mode ui video notes:
    if(doButton){ // draws button, returns true if it was clicked
        action(); //stuff i wanna do if the button was clicked
    }
    uiContext: // tracks the users interaction at given time
        hot:id, // about to be interacted with gets highlighter, pops out etc. no idead if i need this
        active: id, // is interacted with
        // store any other needed data for the active widget(only store data for its active period)

    id:  // some sort of way to identify an element
        // global identifier (pointer to function that does the stuff)
        // or pointer to the data it uses
        owener int, // group of elements
        item int,
        index int,

    //this is a version for mouse
    bool DoButton(uiContext, id, text, pos etc..){
        if active { // compare active id in context with mine
            if mousewentup {
                if hot{
                    result = true
                }
                set not active
            }
        }elseif hot {
            if mousewentdown{
                set active
            }
        }

        if inside {
            set hot// if there is an active item, it doesent set a hot item
        }
        
        drawButton();
    }

    for menus {
        for items{
            doItem();
        }
    }
    for popups{
    }


memory => ui manager => level generation => pathfinding (need to know how the levels are generated before doing the pathfinding thorugh the world)

do i want to learn shaders?

TODO: figure out memory management first, finalize how iam gonna do memory,
main reason is the zig 0.15 change, cant continue without proper memory management, 
would have to rewrite alot of shit later



TODO: make the uimanager immeadeate mode, fix the ui, the puppet select is closing
TODO: arena allocators?? / preallocated ram

what should be the update order? do i have problems because of update order? not all entities are updated at the same time

gonna need to refactor the whole game / context mess, research jblow first
    hardcode ui?
    add time meassurements (ingame, wallclock), always use ingame for mechanics stuff(pause exploit)
    make things proper immidietly, dont put it off like i always do(how jblow made menu)




have a look at how jblow codes, see what i can learn from it, make notes:
    most bugs are because the state isnt what you think it is, thats why its a good idea to not split into smaller functions(you cant call them)

start using the profiller that i made in computer enhance, start to make decisions based on actual numbers

0. UI final fantasy like ui, only use keyboard, dont do mouse
1. Refactor state machine, split out functions a lillte bit more(handle funcitons are too big), refactor input system
2. Mouse input
3. start using ids, instead of sending pointer, dangerous if an entity dies or something
4. better error handling(sometimes I check error in the caller, sometimes in the callee)
5. magic numbers





figure out how to work with assets(tiles) better

random generation world + entities

how to work with enemies, types, how to instantiate, spawn etc





